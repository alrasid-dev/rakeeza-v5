# دليل المستخدم — ركيزة v5

## تسجيل الدخول

1. افتح `/login`.
2. استخدم بريداً ينتهي بـ `@moj.gov.sa`.
3. **الدخول برمز سري من 6 أرقام فقط** (لا كلمة مرور طويلة). الرموز تُدار من `/admin/pins` — لا تُرفع ملفات CSV للإنتاج.
4. **يُسمح بالدخول فقط للحسابات المرتبطة بسجل موظف** (باستثناء مالكي المنصة: `admin@moj.gov.sa` و `snaswig@moj.gov.sa`).
5. زر **«تسجيل موظف جديد»** يفتح نموذجاً يُنشئ طلب تسجيل معلّق يوافق عليه **الرئيس**.
6. **البصمة**: بعد أول دخول بالرمز، من الشاشة الرئيسية فعّل «تفعيل البصمة على هذا الجهاز»؛ بعدها يمكن الدخول بالبصمة من صفحة الدخول (نفس المتصفح/الجهاز).
7. تغيير الرمز من «تغيير كلمة المرور» / إعدادات الرمز — يجب أن يكون 6 أرقام.

## الأدوار

| الدور التقني | التسمية | الصلاحيات باختصار |
|---|---|---|
| **Admin** | **رئيس المحكمة / مالك المنصة** | الحساب الفعلي: `snaswig@moj.gov.sa` — كل القوائم + موافقة التسجيل + **مؤشرات حية** |
| **CourtManager / Secretary** | **الأمين** | أدوات مفيدة (موظفون، تدقيق، استيراد، مساعد) — **بدون** لوحة مؤشرات حية أو تقارير KPI |
| **Judge** | قاضي | مكاتبات وقوالب — واجهة رئيسية خفيفة |
| **Employee** | موظف | رئيسية خفيفة: مستند جديد · أرشيفي · دليلي |

## الشاشة الرئيسية

- **الموظف/القاضي/الأمين**: بطاقات قليلة فقط (مستند جديد، أرشيفي، دليلي) دون ازدحام — بدون ChartsPlaceholder.
- **رئيس المحكمة (المالك) فقط**: بطاقات مؤشرات حية (اليوم/الأسبوع/المعلّق/حسب النوع) مع رسوم بيانية من قاعدة البيانات.

## الشريط الجانبي ومساحة العمل

- الشريط منظم كشجرة قابلة للطي: **النماذج** (كل أنواع المكاتبات) و**المكتبة** (القوالب، الأرشيف، الدليل، الاستيراد/المساعد حسب الصلاحية) و**إدارة** للرئيس فقط.
- اختيار نموذج يفتح مساحة العمل عبر `/documents/new?form=…&name=…` مع شريط سياق يوضح النموذج النشط.
- تُحفظ مسودات الحقول محلياً في `sessionStorage` حسب نوع النموذج حتى لا تُفقد عند التنقل بين النماذج.

## إنشاء مكاتبة

1. من «مستند جديد» اختر قالباً (اختياري) — يشمل **نموذج تحليل حكم (شكوى)** الفارغ بهوية الوزارة.
2. الصق النص في صندوق اللصق الذكي لتوزيع الحقول.
3. احفظ كمسودة أو أصدر برقم `صادر-{سنة}-{تسلسل}`.
4. بعد الإصدار يظهر QR لصفحة التحقق `/verify/[number]`.
5. صدّر **DOCX** بهوية رسمية (ترويسة خضراء/ذهبية، شريط بسم الله، جداول أطراف، لافتات الوقائع/الأسباب، تذييل للاستخدام الداخلي فقط) أو PDF/HTML لـ Outlook.

## القوالب

- صفحة `/templates` مبسّطة: شبكة بطاقات كبيرة (الاسم + زر **استخدام** فقط)، مجمّعة بخفة: **خطابات / حرة / هوية**.
- زر الاستخدام يفتح `/documents/new?form=…&name=…` عند تطابق الاسم مع نموذج معروف.
- قوالب الهوية (تحليل حكم، توقيع رقمي، غلاف) ضمن مجموعة «هوية».

## الموظفون والدليل

- أضف يدوياً أو استورد Excel (أعمدة: الاسم، البريد، الجوال، الهوية).
- الدليل يعرض اللقب التشريفي مع الاسم.
- طلبات التسجيل: `/admin/registrations` للرئيس.

## الاستيراد الذكي والمساعد

- ارفع DOCX/XLSX/TXT لاستخراج النص وتصنيف محلي.
- المساعد يعمل محلياً؛ اختياري عبر OpenAI.

## الإدارة (رئيس المحكمة / المالك)

- وحدات تنظيمية، مناصب/ألقاب، ترقيم، ترويسة/تذييل.
- **رموز الدخول** من `/admin/pins`: تعيين/إعادة تعيين رمز ٦ أرقام للمستخدمين (مالك المنصة = رئيس المحكمة). لا تُرفع ملفات CSV للإنتاج.
- سجل تدقيق لكل العمليات المهمة.
- المراجع النظامية: روابط/iframe من moj.gov.sa فقط.
- **KPIs والإحصاءات الحية للرئيس فقط.**

## ملاحظات

- **KPIs للرئيس فقط**؛ الرموز تُدار من `/admin/pins`؛ القوالب مبسطة.
- لا تُزرع أسماء أطراف قضايا حقيقية.
- نص التعميم الرسمي فارغ عمداً.
- لا يوجد تكامل Entra/Azure في هذا الإصدار.
